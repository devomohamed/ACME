import { HeaderappearDirective } from './headerappear.directive';

describe('HeaderappearDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderappearDirective();
    expect(directive).toBeTruthy();
  });
});

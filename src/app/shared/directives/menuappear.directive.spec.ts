import { MenuappearDirective } from './menuappear.directive';

describe('MenuappearDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuappearDirective();
    expect(directive).toBeTruthy();
  });
});
